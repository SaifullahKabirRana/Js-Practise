// Task-1
// Take four parameters. Multiply the four numbers and then return the result

function numberMultiply(num1, num2, num3, num4) {
    const Multiply = num1 * num2 * num3 * num4;
    return Multiply;
}
 
const result = numberMultiply(4, 2, 3, 2);
console.log(result);

