/**
 * VARIABLE ==> Container
 */
//  number
var sunGlass = 80;

// string
var subject = "EEE";

// boolean 
var passed = true;

/**
 * NAMING CONVENTION 
 * 1. single word
 * 2. no quote 
 * 3. no gap or dash 
 * 4. can not start with number 
 * 5. can not be a keyword
 * 6. prefer camel case 
 */
 
var lastExamResult = 90;
 /**
  * OPERATION
  * 1. + , -, *, /, %
  * 2.+=, -=, *=, /=
  * isNaN ?
  * null 
  * number
  */